# Test-only opt-in composition for the Windows credential Provider's real Loader path.
- id: user-questions
  name: '@deepseek-ai/dsh-user-questions'

- id: credentials
  name: '@deepseek-ai/dsh-credentials-windows-dpapi'
  config: {}
